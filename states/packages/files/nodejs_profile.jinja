#!/usr/bin/env bash
# WARNING!! This file is managed by Salt at <{{ source }}>
# All edits will be lost on the next highstate

export PATH=$PATH:{{nodejs_prefix}}/nodjs/node-{{nodejs_version}}-{{nodejs_distro}}